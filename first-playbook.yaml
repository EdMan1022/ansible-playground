---
# Test semaphore UI connectivity
- name: Do something simple in local machine
  hosts: myhosts
  become: true
  tasks:
    - name: Create a directory if it does not exist
      ansible.builtin.file:
        path: /etc/some_directory
        state: directory
        mode: '0755'
